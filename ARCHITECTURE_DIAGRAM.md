# Architecture & Deployment Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER'S BROWSER                          │
└─────────────────────────────────────────────────────────────────┘
                                 ↓
                                 ↓
                    ┌────────────────────────┐
                    │  VERCEL (Frontend)     │
                    │  React + Vite          │
                    │  https://xxx.vercel.app│
                    └────────────────────────┘
                                 ↓
                    (HTTP REST API Calls)
                                 ↓
                    ┌────────────────────────────┐
                    │  RAILWAY (Backend)         │
                    │  Django REST Framework     │
                    │  https://xxx.railway.app   │
                    └────────────────────────────┘
                                 ↓
                    ┌────────────────────────────┐
                    │  PostgreSQL Database       │
                    │  (on Railway)              │
                    └────────────────────────────┘
```

---

## Deployment Flow Diagram

```
                    Your GitHub Repository
                           │
                   ┌───────┴────────┐
                   │                │
          ┌────────▼────────┐   ┌──▼──────────────┐
          │  RAILWAY        │   │  VERCEL         │
          │  Detects Push   │   │  Detects Push   │
          └────────┬────────┘   └──┬──────────────┘
                   │                │
          ┌────────▼────────┐   ┌──▼──────────────┐
          │  Build Backend  │   │  Build Frontend │
          │  - Installs pip │   │  - npm install  │
          │  - Runs migrate │   │  - npm run build│
          └────────┬────────┘   └──┬──────────────┘
                   │                │
          ┌────────▼────────┐   ┌──▼──────────────┐
          │  Deploys to     │   │  Deploys to     │
          │  Railway.app    │   │  Vercel.app     │
          └────────┬────────┘   └──┬──────────────┘
                   │                │
                   └────────┬───────┘
                            │
                     Services Running
                            │
                  ✓ Production Live! ✓
```

---

## Environment Configuration Flow

```
┌─ LOCAL DEVELOPMENT
│  .env (local)
│  VITE_API_URL=http://localhost:8000/api
│  ↓
│  npm run dev (http://localhost:5173)
│
├─ STAGING
│  Could add railway preview branch
│  ...
│
└─ PRODUCTION
   ┌─── RAILWAY (Backend)
   │    Environment Variables:
   │    ├─ SECRET_KEY
   │    ├─ DEBUG=False
   │    ├─ ALLOWED_HOSTS
   │    ├─ DATABASE_URL (auto)
   │    ├─ CORS_ALLOWED_ORIGINS
   │    └─ CSRF_TRUSTED_ORIGINS
   │
   └─── VERCEL (Frontend)
        Environment Variables:
        └─ VITE_API_URL=https://xxx.railway.app/api
```

---

## Request/Response Flow

```
┌──────────────────────────────────────────────────┐
│ USER ACTION: Click "Upload CSV"                  │
└──────────────────────────────────────────────────┘
                     ↓
┌──────────────────────────────────────────────────┐
│ FRONTEND (Vercel - React)                        │
│ - Handles UI interaction                         │
│ - Creates HTTP request with file                 │
└──────────────────────────────────────────────────┘
                     ↓
        HTTP POST /api/upload/ with file
                     ↓
┌──────────────────────────────────────────────────┐
│ BACKEND (Railway - Django)                       │
│ - Receives POST request                          │
│ - Validates file                                 │
│ - Processes CSV                                  │
│ - Saves to PostgreSQL                            │
└──────────────────────────────────────────────────┘
                     ↓
┌──────────────────────────────────────────────────┐
│ DATABASE (PostgreSQL)                            │
│ - Stores equipment data                          │
│ - Returns ID of new dataset                      │
└──────────────────────────────────────────────────┘
                     ↓
        HTTP 200 OK with dataset ID
                     ↓
┌──────────────────────────────────────────────────┐
│ FRONTEND (Vercel - React)                        │
│ - Receives response                              │
│ - Updates state with new dataset                 │
│ - Shows success message                          │
│ - Re-renders dashboard                           │
└──────────────────────────────────────────────────┘
                     ↓
┌──────────────────────────────────────────────────┐
│ USER SEES: Updated dashboard with charts & data  │
└──────────────────────────────────────────────────┘
```

---

## File Structure & Responsibility

```
FOSSEE-ChemicalAPP/
│
├── backend/                           [RAILWAY]
│   ├── requirements.txt               ← Dependencies
│   ├── Procfile                       ← How to run (gunicorn)
│   ├── runtime.txt                    ← Python version
│   ├── railway.json                   ← Railway config
│   ├── manage.py
│   └── equipment_visualizer/
│       ├── settings.py                ← Production config
│       ├── wsgi.py                    ← WSGI entry point
│       └── urls.py
│   └── api/                           ← Main app
│       ├── views.py                   ← API endpoints
│       ├── models.py                  ← Database models
│       └── serializers.py             ← Data format
│
├── frontend-web/                      [VERCEL]
│   ├── package.json                   ← Dependencies
│   ├── vite.config.js                 ← Build config
│   ├── vercel.json                    ← Vercel config
│   ├── .env.production                ← Prod env vars
│   ├── src/
│   │   ├── main.jsx                   ← Entry point
│   │   ├── App.jsx                    ← Main component
│   │   ├── config.js                  ← API configuration
│   │   └── components/                ← UI components
│   └── dist/                          ← Build output (after build)
│
├── DEPLOYMENT_GUIDE.md                ← Comprehensive guide
├── QUICK_DEPLOYMENT_STEPS.md          ← Visual quick start
├── DEPLOYMENT_CHECKLIST.md            ← Checklist
├── DEPLOYMENT_READY.md                ← This summary
└── README.md                          ← Project info
```

---

## Technology Stack

```
┌───────────────────────────────┐
│      FRONTEND (Vercel)        │
├───────────────────────────────┤
│ React 18+                     │
│ Vite (Build Tool)             │
│ React Router (Navigation)      │
│ Axios (HTTP Client)            │
│ Tailwind CSS (Styling)         │
│ Chart.js (Charts)              │
│ Node.js (Runtime)              │
└───────────────────────────────┘

┌───────────────────────────────┐
│      BACKEND (Railway)        │
├───────────────────────────────┤
│ Django 4.2 (Framework)        │
│ Django REST Framework (API)   │
│ PostgreSQL (Database)          │
│ Gunicorn (Server)              │
│ Pandas (Data Processing)       │
│ Matplotlib (Charts)            │
│ Python 3.10 (Runtime)          │
└───────────────────────────────┘
```

---

## Security Features

```
┌─ AUTHENTICATION
│  ├─ User registration endpoint
│  ├─ JWT tokens (access + refresh)
│  ├─ Token validation on API calls
│  └─ 24-hour token expiration

├─ AUTHORIZATION
│  ├─ Authenticated users only for data endpoints
│  ├─ Users can only access their own data
│  └─ Admin-only endpoints protected

├─ CROSS-ORIGIN (CORS)
│  ├─ Whitelisted Vercel domain
│  ├─ Blocks unauthorized origins
│  └─ Configured on Railway backend

├─ CSRF PROTECTION
│  ├─ CSRF tokens for form submissions
│  ├─ Whitelisted trusted origins
│  └─ SameSite cookies

└─ DATA PROTECTION
   ├─ Passwords hashed with Django auth
   ├─ No sensitive data in frontend
   ├─ HTTPS only (Railway + Vercel)
   └─ PostgreSQL secure by default
```

---

## Deployment Zones

```
┌──────────────────────────────────────────────────────────┐
│                      RAILWAY ACCOUNT                     │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────────────────────────────────────┐       │
│  │  Equipment Visualizer Project               │       │
│  ├─────────────────────────────────────────────┤       │
│  │                                              │       │
│  │  🌐 Backend Service (Django)                │       │
│  │     URL: xxx.railway.app                   │       │
│  │     Resources: CPU, Memory, Bandwidth      │       │
│  │                                              │       │
│  │  🗄️  PostgreSQL Database                    │       │
│  │     Automatically linked to Backend       │       │
│  │     Auto-backups enabled                   │       │
│  │                                              │       │
│  └─────────────────────────────────────────────┘       │
│                                                          │
│  💰 Billing: Pay-as-you-go (free tier available)       │
│  📊 Monitoring: Logs, Metrics, Health Checks           │
│  🔧 Management: Environment Variables, Restart        │
│                                                          │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                    VERCEL ACCOUNT                        │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────────────────────────────────────┐       │
│  │  Equipment Visualizer (Web)                 │       │
│  ├─────────────────────────────────────────────┤       │
│  │                                              │       │
│  │  💻 Frontend Deployment (React)             │       │
│  │     URL: xxx.vercel.app                    │       │
│  │     CDN: Global edge locations             │       │
│  │     Auto-deployed from GitHub              │       │
│  │                                              │       │
│  │  📦 Production Build                        │       │
│  │     Minified JS, CSS                       │       │
│  │     Optimized images                       │       │
│  │     Source maps for debugging              │       │
│  │                                              │       │
│  └─────────────────────────────────────────────┘       │
│                                                          │
│  💰 Billing: Free tier generous                        │
│  📊 Monitoring: Analytics, Performance, Errors         │
│  🔧 Management: Deployments, Rollbacks, Domains       │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## CI/CD Pipeline (Automatic)

```
Developer pushes code to GitHub
         ↓
GitHub webhook triggers both:
    ├─ Railway webhook
    │  ├─ Clone repository
    │  ├─ Install dependencies (pip)
    │  ├─ Run migrations
    │  ├─ Collect static files
    │  ├─ Build Docker container
    │  └─ Deploy to Railway
    │     └─ Your site immediately available
    │
    └─ Vercel webhook
       ├─ Clone repository
       ├─ Install dependencies (npm)
       ├─ Build production version
       ├─ Run tests (if configured)
       ├─ Deploy to global CDN
       └─ Your site immediately available
           with preview URL for PRs

Both services run simultaneously
No manual deployment needed!
```

---

## Local vs Production Comparison

```
┌──────────────┬──────────────────┬──────────────────┐
│   Feature    │      LOCAL       │    PRODUCTION    │
├──────────────┼──────────────────┼──────────────────┤
│ Frontend     │ npm run dev      │ Vercel CDN       │
│              │ localhost:5173   │ xxx.vercel.app   │
├──────────────┼──────────────────┼──────────────────┤
│ Backend      │ django dev       │ Railway + Gunicorn│
│              │ localhost:8000   │ xxx.railway.app  │
├──────────────┼──────────────────┼──────────────────┤
│ Database     │ SQLite local     │ PostgreSQL cloud │
│              │ ./db.sqlite3     │ Auto-managed     │
├──────────────┼──────────────────┼──────────────────┤
│ HTTPS        │ No               │ Yes (automatic)  │
├──────────────┼──────────────────┼──────────────────┤
│ Debug Mode   │ Yes (DEBUG=True) │ No (DEBUG=False) │
├──────────────┼──────────────────┼──────────────────┤
│ Logging      │ Console          │ Service logs     │
├──────────────┼──────────────────┼──────────────────┤
│ Static Files │ Served by Django │ Whitenoise CDN   │
├──────────────┼──────────────────┼──────────────────┤
│ Monitoring   │ None             │ Dashboards       │
└──────────────┴──────────────────┴──────────────────┘
```

---

## Summary

This diagram shows:
- ✅ How frontend and backend communicate
- ✅ How deployment works automatically
- ✅ What services run where
- ✅ How data flows through the system
- ✅ Security and configuration

**You're ready to deploy!**

